import { Tabs } from "components";
import { SearchPanel } from "modules";
import "./Orders.css";

export const Orders = () => {
  return (
    <div className="orders">
      <SearchPanel />
      <Tabs />
      orders
    </div>
  );
};
